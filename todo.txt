Current cleanup:

- Preserve the executable bit on templates/gradle-eclipse-java/gradlew.

Deferred:

- Finish the Gradle/Eclipse project-creation helper when a real project needs it.
- Continue the broader WSL migration after higher-priority ChatMap work.
