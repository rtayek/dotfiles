# Windows login profile equivalents

if [ -f /c/Users/ray/miniconda3/etc/profile.d/conda.sh ]; then
  . /c/Users/ray/miniconda3/etc/profile.d/conda.sh
fi

# Prevent Git Bash from forcing a black background in Windows Terminal.
[ -n "${WT_SESSION-}" ] && printf '\e]11;?\e\\'

export RAY_TERMINAL_TITLE_PREFIX=WIN

if [ -f "${HOME}/.bash_functions-windows" ]; then
  . "${HOME}/.bash_functions-windows"
elif [ -f "$HOME/dotfiles/bash/bash_functions-windows" ]; then
  . "$HOME/dotfiles/bash/bash_functions-windows"
fi

export myroot="/c/dfromrays8350"
pathPrepend "$myroot/bin"

export GRADLE_HOME="/c/Gradle/gradle-9.1.0"
pathPrepend "$GRADLE_HOME/bin"

export JAVA_HOME="/c/Program Files/Java/jdk-25"
pathPrepend "$JAVA_HOME/bin"

antigravity_bin="/c/Users/ray/AppData/Local/agy/bin"
[ -d "$antigravity_bin" ] && pathPrepend "$antigravity_bin"
unset antigravity_bin

export MSYS=winsymlinks:nativestrict

