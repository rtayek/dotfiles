pathPrepend() {
  case ":$PATH:" in
    *":$1:"*) : ;;
    *) PATH="$1:$PATH" ;;
  esac
}

dedupePath() {
  PATH="$(printf '%s' "$PATH" | awk -v RS=: '!seen[$0]++ {printf "%s%s", (out++?":":""), $0}')"
}
